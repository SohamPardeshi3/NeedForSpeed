<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.needforspeed">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".placeOrderActivity"></activity>
        <activity android:name=".CartActivity" />
        <activity android:name=".cottonSeedsActivity" />
        <activity android:name=".emptyCartActivity" />
        <activity android:name=".seedsActivity" />
        <activity android:name=".profile" />
        <activity android:name=".otp_login" />
        <activity android:name=".choose_niche" />
        <activity android:name=".buyingScreen" />
        <activity android:name=".sellingScreen" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".buyingScreen" />
        <activity android:name=".introScreen" />
        <activity android:name=".shoppingScreen" />
        <activity android:name=".introScreen3" />
        <activity android:name=".introScreen2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>